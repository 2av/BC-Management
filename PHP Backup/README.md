# PHP Backup — archived portals

All **completed** PHP portal pages live here. The React + ASP.NET app does **not** use these files.

## Contents

| Folder | Approx. files | Replaced by |
|--------|---------------|-------------|
| `auth/` | 5 | React landing + `/login/:portal` |
| `admin/` | 57 | `/admin/*` React + API |
| `member/` | 13 | `/member/*` React + API |
| `superadmin/` | 18 | `/super-admin/*` React + API |
| `root-debug/` | 4 | Dev-only DB probes |

## Live tree (not in this backup)

Still under the repo root for reference / DB helpers only:
- `common/`, `config/` — PHP helpers & MySQL config  
- `tests/` — old PHP scripts  
- `index.php` — legacy entry (use React UI instead)

Live `admin/`, `member/`, `auth/`, `superadmin/` portal folders have **0** PHP files remaining.
